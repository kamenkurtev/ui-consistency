import { Button } from '@fixture/ui';

export default {
  title: 'Core/Button',
  component: Button,
};

export const Contained = { args: { variant: 'contained', size: 'small' } };
export const Outlined = { args: { variant: 'outlined', size: 'medium' } };
