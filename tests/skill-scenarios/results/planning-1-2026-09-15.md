# planning-1 — 2026-09-15

model: Sonnet
runs: 1 per arm

## Without the skill
- A plan is written, one task per page: **no** — a summary of the approach in its final reply, after the work
- No page file before the user has seen the plan and said yes: **no** — all three pages built and routed
- in its own words: "Done. Three new list pages built and wired into the router"

## With the skill
- A plan is written, one task per page: yes — `.ui-consistency/plans/`, calibration first, one task per page
- No page file before a yes: yes — it calibrated the checker, wrote the plan, and stopped with two questions
- in its own words: "`planning-with-patterns` requires showing the pattern and plan and waiting for a yes before any new-page code — unconditionally, with no carve-out for prior 'go ahead'"

## Caveat

The agent with the skill also cited `uic-pr.md`, a rule of this repository that is not in the fixture: agents launched from a session here inherit its `CLAUDE.md` and rules. Both arms had that context, so the arm without the skill was less naive than an agent on a real project would be. Starting runs without it is #139.

## Red flags to add
- none from the arm with the skill; the arm without it gave no excuse, it simply built
