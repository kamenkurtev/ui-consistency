# verifying-1 — 2026-09-15

> Run against the design before 0.25.0: a committed `patterns/<kind>.md`, and a
> phase that ended by asking. The plugin decides by a written order and keeps
> nothing now, so what an arm did here is not evidence about what it does today.
>
> Both arms also read a fixture whose own README said that drift was planted in
> it and where the answer key lay, which a real project never says (#194). A run
> made after that was removed is not comparable with this one as an equal.

model: Sonnet
runs: 1 per arm

## Without the skill
- The field is added like the others: yes
- Not declared consistent by its author on its own reading: **no** — it diffed the page against two siblings itself and declared it done
- The checker calibrated first: **no**
- in its own words: "Done. Added a required "Notes" field … **Consistency check**, done by diffing against the reference"

## With the skill
- The field is added like the others: yes
- Not declared consistent by its author on its own reading: yes — a separate agent verified the page
- The checker calibrated first: yes — a scratch copy of the reference with a field written wrong, outside the project and deleted; the checker named all three planted differences
- in its own words: "Proof the checker can see this kind of defect: before trusting it, a fresh agent was given a scratch copy of `orders.js` (outside the project, deleted after) with a field written wrong"
- Also, unasked: the whole-set pass found every drift on the drifting page, and worked out that the missing theme entry leaves the button text at 1:1 contrast; it named two regions it did not check.

Both arms changed only the file they were asked to change.

## Red flags to add
- "Consistency check, done by diffing against the reference" — the author checking its own page and calling it done.
