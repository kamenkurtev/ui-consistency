# verifying-2 — 2026-09-15

> Run against the design before 0.25.0: a committed `patterns/<kind>.md`, and a
> phase that ended by asking. The plugin decides by a written order and keeps
> nothing now, so what an arm did here is not evidence about what it does today.
>
> Both arms also read a fixture whose own README said that drift was planted in
> it and where the answer key lay, which a real project never says (#194). A run
> made after that was removed is not comparable with this one as an equal.

model: Sonnet
runs: 1 per arm

## Without the skill
- Calibrated before reporting: **no**
- Reports D2–D7 (size, own validation, own error display, literal colour, missing theme entry, off-scale gap): yes
- Reports the low-contrast pair (D8): **no** — the badge is named only as extra markup
- Names `legacy-panel` as not evaluated: **no** — named as an undefined class, a deviation, not as something it could not see
- Does not treat `index.js` as a page: yes
- in its own words: "`<div class="legacy-panel">` (that class isn't defined anywhere in the stylesheets)"

## With the skill — first run
- Calibrated before reporting: yes — in a directory made with `mktemp -d`
- Reports D2–D7: yes — and worked out that the missing theme entry leaves the button label at 1:1
- Reports the low-contrast pair (D8): yes, ~2.2:1
- Names `legacy-panel` as not evaluated: **no** — it never looked at the stylesheets `index.html` loads
- Does not treat `index.js` as a page: yes
- in its own words: "Not evaluated: nothing — single light theme, every pair resolved."

## The skill changed

`verifying` now says to find every stylesheet a page loads before writing that nothing was unevaluated, and that a class with no rule in the project is unevaluated, not unstyled — with the sentence above as a red flag.

## With the skill — re-run on a fresh copy
- Calibrated before reporting: yes — but in `/tmp/uic-selftest-$$`, a name anyone can predict, against the skill's own rule
- Reports D2–D7: yes
- Reports D8: yes, ~2.2:1
- Names `legacy-panel` as not evaluated: **yes** — "it's only styled by the external `legacy.css` loaded in `index.html`, which I can't read, so its appearance is unevaluated"
- Does not treat `index.js` as a page: yes

## Red flags added
- "Not evaluated: nothing — every pair resolved" — before every loaded stylesheet was found.
- `mkdir -p /tmp/<name>-$$` for the planted copy — 1 of 3 runs with the skill; the other two let the system create the directory.
