{
  "description": "ui-consistency — skills and rules that keep the screens an agent writes consistent with the ones a project already has",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/bin/uic.mjs\" session",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
