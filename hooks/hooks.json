{
  "description": "An AI-aware design system, read from the code you already have: your coding agent writes each page the way your other pages are built",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/bin/uic.mjs\" session",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
